// import React, { useState } from 'react';
// import { ItemConfig } from '../../interface/group/UserGroupType';

// const [dataConfig, setDataConfig] = useState([]);
// const [createConfig, setCreateConfig] = useState([]);
// const [modifierConfig, setModifierConfig] = useState([]);
// const [managerConfig, setManagerConfig] = useState([]);

// export const defaultData = dataConfig.map((item: ItemConfig) => {
// 	return { display: item.displayText, value: item.value };
// })[1]?.display;

// export const defaultCreate = createConfig.map((item: ItemConfig) => {
// 	return { display: item.displayText, value: item.value };
// })[1]?.display;

// export const defaultModifier = modifierConfig.map((item: ItemConfig) => {
// 	return { display: item.displayText, value: item.value };
// })[1]?.display;

// export const defaultManager = managerConfig.map((item: ItemConfig) => {
// 	return { display: item.displayText, value: item.value };
// })[1]?.display;
export const titleFunction = [
	{
		title: 'Danh mục quản lý',
		key: 1,
	},
	{
		title: 'Xem',
		key: 2,
	},
	{
		title: 'Tạo mới',
		key: 3,
	},
	{
		title: 'Sửa',
		key: 4,
	},
	{
		title: 'Quản lý khác (Xóa, import...)',
		key: 5,
	},
];
